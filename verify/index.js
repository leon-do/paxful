const paypalEmail = require('./paypalEmail')
const paypalPayment = require('./paypalPayment')
const userReputation = require('./userReputation')

module.exports = { paypalEmail, paypalPayment, userReputation }
